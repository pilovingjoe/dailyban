info:
  name: Successful Login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:9812/login
  body:
    type: json
    data: |-
      {
        "email":"test@test.gov",
        "password":"password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
